# Git files
.git
.gitignore

# Node modules - These will be installed inside the container
node_modules

# Build output - This is generated inside the container
build

# OS generated files
.DS_Store
Thumbs.db

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files (if not needed in build context, .env is usually needed)
# .env # Keep this line commented if .env is needed during build

# Test files (if not needed for build)
coverage